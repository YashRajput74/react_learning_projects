export function parseFormattedText(text) {
    const tokens = [];
    const regex = /(\*[^*]+\*|_[^_]+_|~[^~]+~|`[^`]+`|\[[^\]]+\]\([^)]+\))/g;
    let lastKey = 0;
    let keyIndex = 0;

    while((match))
}